import math

base = int(input('Digite a base: '))
elevado = int(input('Digite o valor elevado: '))

resultado = int(math.pow(base,elevado))

print(f' O resultado de {base} elevado a {elevado} é igual á: {resultado}')

